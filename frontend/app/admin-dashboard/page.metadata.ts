import { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'Admin Dashboard - Authentico',
  description: 'Administrative dashboard for Authentico platform',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
};
