'use client';

import { NeubrutalistLoading } from './components/ui/NeubrutalistLoading';

export default function Loading() {
  return <NeubrutalistLoading message="Loading Authentico" fullScreen={true} />;
}
