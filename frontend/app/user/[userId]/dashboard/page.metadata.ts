import { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'User Dashboard - Authentico',
  description: 'User dashboard for Authentico platform',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
};
